import React, { ReactElement } from 'react';

import c from './Login.module.css';

/* type PropsType = {
  // login: string
  // isAuth: boolean
}; */

export const Login = (): ReactElement => <div className={c.login}>log in page</div>;
